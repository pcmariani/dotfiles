rgfzf() {
  command rg --color=always --line-number --no-heading --smart-case \
    -g '!Library' -g '!site-packages' -g '!raycast' "${*:-}" \
    | command fzf -d':' --ansi \
        --preview "command bat -p --color=always {1} --highlight-line {2}" \
        --preview-window ~8,+{2}-5 \
    | awk -F':' '{print $1}'
}

rgr() {
  rg --files --hidden -g "$1"
}

youtube-download-transcript() {
  yt-dlp --skip-download --write-subs --write-auto-subs --sub-lang en --sub-format vtt --output "$2.transcript.%(ext)s" $1
  sed -i '' -e "1,4d" -e "/^$/d" -e "/^[0-9]\{2\}.*/d" -e "s/\(&nbsp;\)\{1,\}$//" "$2.transcript.en.vtt"
  # on mac you need '' after -i
}

yadm-lazygit() {
  pushd "$HOME"
  yadm enter lazygit
  popd
}

mkdircd() { \mkdir -p -- "$1" && \cd -P -- "$1"; }

# # do ls after cd
# function chpwd() {
#     emulate -L zsh
#     ls -a
# }

# sesh-sessions() {
#   {
#     exec </dev/tty
#     exec <&1
#     local session
#     session=$(sesh list -i | gum filter --limit 1 --no-sort --fuzzy --no-strip-ansi --placeholder 'Pick a sesh' --height 50 --prompt='⚡')
#     zle reset-prompt > /dev/null 2>&1 || true
#     [[ -z "$session" ]] && return
#     sesh connect $session
#   }
# }


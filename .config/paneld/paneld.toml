# paneld. One panel per [[panel]] block.
[[panel]]
name    = "picker"
# cmd-esc, replacing ctrl-space on 2026-09-09. VERIFIED by hand with both the
# physical Esc key and caps_lock.
#
# caps_lock works even though karabiner.edn maps it to left_control in
# combination and escape only when tapped alone
# (`[:##caps_lock :left_control nil {:alone :escape}]`): the `##` prefix
# passes the held cmd through, so the tap emits cmd+escape.
#
# And that chord is Karabiner-SYNTHESIZED, yet it reaches paneld's Carbon
# registration -- which narrows karabiner.edn's note about Karabiner rewriting
# below the layer Carbon registers against. That failure was specific to
# ctrl-shift-space -> hyper-P; it is not a general rule.
#
# A plain chord on purpose, not a space-mode layer entry: a layer would have to
# shell out to `paneld toggle picker`, paying a round trip plus space-mode's
# ~160ms hold threshold on the key pressed most often.
#
# CHANGING A HOTKEY NEEDS A DAEMON RESTART, not `paneld reload`:
# registerHotkey runs only in the daemon's startup loop (Daemon.swift:81), and
# `paneld status` prints the CONFIG's hotkey rather than the registered one --
# so it will confidently report a hotkey that Carbon does not hold.
#     launchctl kickstart -k "gui/$(id -u)/dev.paneld.paneld"
hotkey  = "cmd-esc"
compose = "context pick-command contexts"
width   = 900
height  = 420
arm     = true

# At reveal, paneld presses F12 in this panel's live pty. compose() binds F12 to
# `reload-sync(picker-rows.sh)`, so the agent-status cell refreshes ~200ms into
# the open -- while you are already reading -- and the keypress itself still
# only reveals a pre-drawn surface.
#
# F12 and not ctrl-n: ctrl-n is one of fzf's own defaults for `down`. f12
# resolves to macOS keycode 0x6F in libghostty's pinned table, so sendKey will
# not take its silent false path.
#
# IDEMPOTENT POKES ONLY. A late press lands in whatever pty is live when it
# arrives, so nothing with side effects may ever be bound here -- no verb, no
# execute-silent, nothing that writes. A reload is safe because running it
# twice is harmless.
on_reveal_key = "f12"
[[panel]]
name    = "move"
# shift-cmd-esc, next door to the picker's cmd-esc. A plain chord, so paneld
# registers it with Carbon itself -- see the note on the picker's hotkey above.
hotkey  = "shift-cmd-esc"
compose = "context pick-command move"
width   = 900
height  = 420
arm     = true

# At reveal, paneld presses F12 in this panel's live pty. compose() binds F12 to
# `reload-sync(picker-rows.sh)`, so the agent-status cell refreshes ~200ms into
# the open -- while you are already reading -- and the keypress itself still
# only reveals a pre-drawn surface.
#
# F12 and not ctrl-n: ctrl-n is one of fzf's own defaults for `down`. f12
# resolves to macOS keycode 0x6F in libghostty's pinned table, so sendKey will
# not take its silent false path.
#
# IDEMPOTENT POKES ONLY. A late press lands in whatever pty is live when it
# arrives, so nothing with side effects may ever be bound here -- no verb, no
# execute-silent, nothing that writes. A reload is safe because running it
# twice is harmless.
on_reveal_key = "f12"
[[panel]]
name    = "files"
# Summoned by space-shift-f, which Karabiner routes to `paneld toggle files`
# over the control socket -- a space-mode layer cannot hand Carbon a chord it
# did not receive itself.
#
# So this hotkey is NEVER PRESSED, and it is set explicitly because the field
# defaults to `f18` when omitted -- and karabiner.edn's retired "context
# picker" rule used f18, so inheriting that default would re-grab a key with
# history. hyper-f20 needs all four modifiers plus a key most keyboards do not
# have.
hotkey  = "hyper-f20"
compose = "context pick-command files"
width   = 900
height  = 420
# NOT armed, unlike every other panel. Arming RUNS the composed command so the
# surface is drawn and ready -- and this producer's rows depend on WHICH
# WORKSPACE IS FOCUSED, which at daemon-start time is whatever it happens to
# be. If that workspace has no root the producer correctly says "nowhere to
# look", so the first invocation drew an empty list and only the second worked
# (reported 2026-09-09).
#
# The alternative was rearming this panel from picker-refresh.sh on every
# workspace change, like `picker`. Rejected: that runs `fd` over the workspace
# on every focus change, and a client root is ~2,652 files -- paying a
# directory walk constantly for a picker opened occasionally.
#
# So the producer runs at invocation instead. `fd` is fast (89 files in this
# repo, 2,652 in a client root) and the rows are correct by construction
# rather than correct-if-rearmed.
arm     = false

[[panel]]
name    = "config-files"
# Summoned by space-shift-c via `paneld toggle`, like the files picker -- so
# this hotkey is never pressed and is set explicitly only to avoid the f18
# default. See the files panel above.
hotkey  = "hyper-f19"
compose = "context pick-command config-files"
width   = 900
height  = 420
arm     = true

[[panel]]
name    = "switcher"
# cmd-tab, taken from the Dock by karabiner.edn and rewritten to cmd-f18.
# The `to` binding KEEPS cmd deliberately: release-to-commit depends on the
# modifier still being physically down and visible to a local NSEvent
# monitor. A bare f18 would drop it and the panel would never commit.
#
# CHANGING A HOTKEY NEEDS A DAEMON RESTART, not `paneld reload` -- and note
# that `cycle` now has the same requirement for a different reason: the
# hotkey's branch reads the controller's live config, but the CHORD itself is
# registered once at startup.
hotkey         = "cmd-f18"
hotkey_reverse = "cmd-shift-f18"
cycle          = true
# TODO: switch to `context pick-command switcher` once the live-only producer
# lands in ~/projects/context-based-mac. `contexts` is the full ~40-workspace
# list and is the wrong list for cycling -- but it is real rows, which is what
# the mechanism needs to be exercised against.
compose        = "context pick-command switcher"
width          = 900
height         = 420
arm            = true
